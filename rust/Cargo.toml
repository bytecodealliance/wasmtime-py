[workspace]
members = ['bindgen']
resolver = "2"

[workspace.dependencies]
wit-bindgen = { version = "0.19.1", default-features = false, features = ['macros'] }
wit-bindgen-core = "0.19.1"

[profile.release]
strip = 'debuginfo'
